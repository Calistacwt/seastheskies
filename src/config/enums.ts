export enum SortBy {
  Latest = 'latest',
  Earliest = 'earliest',
}

export enum Actions {
  Access = 'access',
  Manage = 'manage',
}

export enum Abilities {
  Dashboard = 'D',
  ExampleRead = 'ER',
  ExampleCreate = 'EC',
  ExampleUpdate = 'EU',
  ExampleDelete = 'ED',
}
